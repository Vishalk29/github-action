name: Basic CI
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: set up dependancy
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      # - name: Install dependancies
      #   run: npm ci

      # - name: Create the production build
      #   run: npm run build
